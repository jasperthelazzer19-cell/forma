"""Forma — ACT prep, methodically.

Package layout:
    shell.py         — colors, fonts, HEAD, sidebar, palette, section labels
    db.py            — SQLite connection helpers, first-boot seeding
    landing.py       — public landing page (and dev variants /v1-/v5)
    explanations.py  — fetcher for legacy crackab.com explanations
"""
